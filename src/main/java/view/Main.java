package view;

/**
 * The Main class in the view package serves as the entry point for launching the JavaFX user interface application by calling the launch method on UusiGui.class.
 */
public class Main {
    /**
     * The main method serves as the entry point for the JavaFX application.
     * It calls the launch method on the UusiGui.class to start the user interface.
     * @param args The command line arguments passed to the application. Not used in this application.
     */
    public static void main(String[] args) {UusiGui.launch(UusiGui.class);
    }
}